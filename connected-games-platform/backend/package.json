{
  "name": "connected-games-backend",
  "version": "1.0.0",
  "description": "Microservizi didattici per la piattaforma Giochi Connessi",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:service": "node serviceServer.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check serviceServer.js && node --check mqttClient.js && node --check schemaBootstrap.js && node --check controllers/*.js && node --check routes/*.js && node --check services/*.js && node --check utils/*.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mqtt": "^5.10.3",
    "mysql2": "^3.11.5"
  }
}
